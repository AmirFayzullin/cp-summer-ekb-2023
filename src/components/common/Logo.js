export const Logo = ({fontSize = 15}) => {
    return (
        <p style={{
            color: 'grey',
            fontWeight: 600,
            margin: '0',
            fontSize: `${fontSize}px`,
            textAlign: 'center'
        }}>
            FastDocs
        </p>
    )
};